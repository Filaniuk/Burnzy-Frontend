export default function SectionTitle({ title }) {
  return (
    <h2 className="text-2xl font-bold mb-5 bg-gradient-to-r from-[#6C63FF] to-[#00F5A0] bg-clip-text text-transparent">
      {title}
    </h2>
  );
}